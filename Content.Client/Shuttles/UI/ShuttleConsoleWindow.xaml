<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:ui="clr-namespace:Content.Client.Shuttles.UI"
                      xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      Title="{Loc 'shuttle-console-window-title'}"
                      SetSize="1000 1000"
                      MinSize="640 640"> <!-- Forge-Change -->
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True"> <!-- Forge-Change -->
        <PanelContainer VerticalExpand="True" HorizontalExpand="True" Margin="0 8 0 2">
            <PanelContainer.PanelOverride>
                <graphics:StyleBoxFlat BorderThickness="7" BorderColor="#252525" BackgroundColor="#3d474d"/>
            </PanelContainer.PanelOverride>
            <PanelContainer VerticalExpand="True" HorizontalExpand="True" Margin="4 4 4 4">
                <PanelContainer.PanelOverride>
                    <graphics:StyleBoxFlat BorderThickness="2" BorderColor="#333333" BackgroundColor="#181818"/>
                </PanelContainer.PanelOverride>
        <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True"> <!-- Forge-Change -->
        <!-- Top row mode buttons -->
        <PanelContainer VerticalExpand="False" HorizontalExpand="True"> <!-- Forge-Change -->
            <PanelContainer.PanelOverride>
                <graphics:StyleBoxFlat BorderThickness="2" BorderColor="#333333" BackgroundColor="#121212"/>
            </PanelContainer.PanelOverride>
        <BoxContainer Name="ModeButtons"
                       Orientation="Horizontal"
                       HorizontalAlignment="Stretch"
                       HorizontalExpand="True"
                       MinSize="52 52"
                       Margin="5">
            <Button Name="NavModeButton"
                    ToggleMode="True"
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    Text="NAV"
                    StyleClasses="ButtonSquare"/>
            <Button Name="MapModeButton"
                    ToggleMode="True"
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    Text="MAP"
                    StyleClasses="ButtonSquare"/>
            <Button Name="DockModeButton"
                    ToggleMode="True"
                    HorizontalExpand="True"
                    VerticalExpand="True"
                    Text="DOCK"
                    StyleClasses="ButtonSquare"/>
        </BoxContainer>
        </PanelContainer>
        <!-- Contents box -->
        <!-- Forge-Change-Start -->
        <BoxContainer Name="Contents"
                      HorizontalAlignment="Stretch"
                      HorizontalExpand="True"
                      VerticalExpand="True"
                      Margin="5">
            <ui:NavScreen Name="NavContainer" Visible="False" HorizontalExpand="True" VerticalExpand="True"/>
            <ui:MapScreen Name="MapContainer" Visible="False" HorizontalExpand="True" VerticalExpand="True"/>
            <ui:DockingScreen Name="DockContainer" Visible="False" HorizontalExpand="True" VerticalExpand="True"/>
        <!-- Forge-Change-End -->
        </BoxContainer>
        </BoxContainer>
            </PanelContainer>
        </PanelContainer>
    </BoxContainer>
</controls:FancyWindow>
